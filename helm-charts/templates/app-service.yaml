apiVersion: v1
kind: Service
metadata:
  name: {{ include "swimlane-app.app.fullname" . }}
  labels:
    {{- include "swimlane-app.app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.app.service.type }}
  ports:
    - port: {{ .Values.app.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "swimlane-app.app.selectorLabels" . | nindent 4 }}

